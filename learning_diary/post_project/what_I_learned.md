1. there is so many ways to organize the project and code and managing errors is very flexible 
2. I did not follow the rule function should have one job now data_cleaning modeles function clean_data() 
   1. does more than 1 thing it parses data and cleans it for the dataframe then creates dataframe out of the cleaned data 
   2. it could be 2 different functions 
      1. parse_response() and clean_data